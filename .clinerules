# 世界杯球迷中心 - 设计原则约束

本文档中的原则在每次对话中始终有效，所有代码修改必须遵守。

---

## 核心哲学

**"惊艳"，不是"能用"。** 每个像素都有意图，每个交互都是精心设计的。尊重设计系统与品牌一致性，同时敢于创新。

---

## 1. 设计系统先行（Step 3）

在写任何代码之前，先用 Markdown 声明设计系统：
- 色彩方案：主色 / 辅助色 / 中性色 / 强调色
- 排版：标题字体 / 正文字体 / 代码字体
- 间距系统：基础单位及倍数
- 圆角策略：大圆角 / 小圆角 / 锐利
- 阴影层级：海拔 1-5
- 动效风格：缓动曲线 / 持续时间 / 触发方式

使用 CSS 自定义属性管理设计 tokens，必要时用 `oklch()` 派生和谐变体。

---

## 2. 避免 AI 风格陈词滥调

**必须避免：**
- 紫色-粉色-蓝色渐变背景
- 左侧彩色边框的圆角卡片
- 用 SVG 绘制复杂图形（用占位符并请求真实资源）
- 千篇一律的渐变按钮 + 大圆角卡片组合
- 过度使用 Inter、Roboto、Arial、Fraunces、system-ui 字体
- 无意义的统计数字 / 图标堆砌（"数据淤泥"）
- 伪造的客户 logo 墙或虚假推荐数量

---

## 3. 表情符号规则

**默认不使用 emoji。** 只在目标品牌/设计系统本身使用 emoji 时才用（如 Notion、早年的 Linear），且必须精确匹配其密度和上下文。

- ❌ 用 emoji 替代图标（"没有图标库，用 🚀 ⚡ ✨ 填充"）
- ❌ 用 emoji 作为装饰填充
- ✅ 没有图标 → 使用占位符（如 `[icon]`、`▢`），表明需要真实图标
- ✅ 品牌本身用 emoji → 遵循品牌

---

## 4. 占位符哲学

**当你缺少图标、图片或组件时，占位符比粗糙的假造更专业。**

| 缺失内容 | 占位方式 |
|----------|----------|
| 图标 | 方块 + 标签（`[icon]`、`▢`） |
| 头像 | 首字母圆圈 + 颜色填充 |
| 图片 | 占位卡片 + 宽高比信息（如 `16:9 image`） |
| 数据 | 主动向用户索取，绝不捏造 |
| Logo | 品牌名称文字 + 简单几何形状 |

**占位符 = "这里需要真实材料"。伪造 = "我偷工减料了"。**

---

## 5. 内容原则

- **无填充内容**：每个元素必须有存在的理由
- **不单方面添加页面/区块**：如需更多内容，先询问用户
- **占位符 > 伪造数据**：虚假数据比承认缺口更能损害可信度
- **少即是多**：留白就是设计
- 如果页面看起来空 → 这是布局问题，不是内容问题。用构图、留白和字号节奏解决，而非塞入内容。

---

## 6. CSS 最佳实践

- 布局优先 CSS Grid + Flexbox
- 用 CSS 自定义属性管理设计 tokens
- 使用 `text-wrap: pretty` 优化换行
- 使用 `clamp()` 实现流体排版
- 使用 `@container` 查询实现组件级响应
- 兼顾 `@media (prefers-color-scheme)` 和 `@media (prefers-reduced-motion)`
- 关键交互添加 CSS transition/animation
- 善用 SVG filters、`backdrop-filter`、`mix-blend-mode`、`mask` 等高级 CSS

---

## 7. React 三大不可协商规则

使用 React 原型时：

1. **禁止使用 `const styles = { ... }`** — 多组件文件会让 `styles` 全局变量互相覆盖。必须带组件名前缀：
   ```jsx
   const terminalStyles = { container: { ... } };
   ```

2. **分离的 `<script type="text/babel">` 块不共享作用域** — 跨文件组件必须显式挂载到 `window`：
   ```jsx
   Object.assign(window, { Terminal, Line });
   ```

3. **禁止使用 `scrollIntoView`** — 在 iframe 嵌入预览中会破坏外层滚动。使用 `element.scrollTop = ...` 或 `window.scrollTo({...})` 替代。

---

## 8. 输出规模参考

| 上下文 | 最小尺寸 |
|--------|----------|
| 1920×1080 演示 | 正文 ≥ 24px |
| 移动端原型 | 触控目标 ≥ 44px |
| 打印文档 | ≥ 12pt |
| Web 正文 | 起始 16–18px |

---

## 9. 模块化文件管理

- 使用描述性文件名
- 超过 1000 行的文件拆分为多个小组件
- 重大修改时保留旧版本（`v2`/`v3` 命名）
- 多方案变体优先用 **单文件 + Tweaks 面板切换**，而非多文件

---

## 10. 交付前检查清单

- [ ] 浏览器控制台无错误、无警告
- [ ] 目标设备/视口渲染正确
- [ ] 交互组件覆盖状态：hover / focus / active / disabled / loading / empty / error
- [ ] 无文本溢出或截断，`text-wrap: pretty` 已应用
- [ ] 所有颜色来自已声明的设计系统 — **无外来色调**
- [ ] 未使用 `scrollIntoView`
- [ ] React 项目：无 `const styles = {...}`，跨文件组件已导出到 `window`
- [ ] 无 AI 陈词滥调（紫粉渐变、emoji 滥用、左边框强调卡片、Inter/Roboto）
- [ ] 无填充内容、无伪造数据
- [ ] 语义化命名、结构清晰、易于后续修改
- [ ] 视觉质量达到 Dribbble / Behance 展示级别

---

## 11. 设计上下文优先级

修改/新增 UI 时，先理解现有视觉词汇再动手：

1. **优先读取用户提供的资源**（截图 / Figma / 代码库）
2. **审视项目现有页面** → 提取已有的设计 tokens
3. **行业最佳实践** → 询问用户参考品牌或产品
4. **从零开始** → 明确告知用户"无参考将影响最终质量"

分析要点：色彩系统、排版方案、间距系统、圆角策略、阴影层级、动效风格、组件密度、文案语调。

**新增元素应与现有元素水乳交融，无法区分。**